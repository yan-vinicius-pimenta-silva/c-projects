#include <stdio.h>

int main(){

    // arithmetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement


    int x = 5; // %d é utilizado para imprimir valores do tipo int
    int y = 2;

    float z = x % y;

    x++;
    y--;

    printf("%d", y);
     



    return 0;
}